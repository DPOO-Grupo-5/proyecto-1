package galeria.modelo.usuario;

/**
 * Indica el rol que tiene cada usuario dentro de la galeria, se clasifica segun las acciones que pueden realizar.
 */
public enum Rol {
	ADMINISTRADOR, CAJERO, OPERADOR, CLIENTE
}
